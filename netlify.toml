# netlify.toml (build section)
[build]
  command = "npm install && npm run build -- --mode web"
  publish = "dist"

[[headers]]
  for = "/src/*"
  [headers.values]
    Content-Type = "application/javascript"
    X-Content-Type-Options = "nosniff"
